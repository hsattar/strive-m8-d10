    // it('Should be able to let a user log in with their details', async () => {
    //     const response = await request.post('/users/login').send(validLogin) 
    //     cookies.accessToken = response.header['accessToken']
    //     cookies.refreshToken = response.header['refreshToken']
    //     console.log(cookies.accessToken)
    //     expect(cookies.accessToken).not.toBe('')
    //     expect(cookies.refreshToken).not.toBe('')
    //     // expect(response.status).toBe(200)
    //     // expect(response.body).toBe('Tokens Sent')
    // })